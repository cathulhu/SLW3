<?xml version="1.0" encoding="utf-8"?>

<!-- Layout for individual news entries in a list -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <!-- Icon shown next to the title/subtitle -->
    <ImageView
        android:id="@+id/profile_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:padding="3dp" />

    <!-- Profiles Values -->
    <TextView
        android:id="@+id/profile_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/profile_icon"
        android:layout_alignTop="@id/profile_icon"
        android:layout_margin="5dp"
        android:textSize="14sp"
        android:textStyle="bold" />

    <!-- Subtitle with profile attributes -->
    <TextView
        android:id="@+id/profile_income"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/profile_name"
        android:layout_below="@id/profile_name"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/profile_spouse_income"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/profile_income"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/profile_tax_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/profile_spouse_income"
        android:textSize="12sp" />

</RelativeLayout>